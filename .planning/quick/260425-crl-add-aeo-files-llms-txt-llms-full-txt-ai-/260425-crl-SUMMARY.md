---
quick_id: 260425-crl
description: Add AEO files (llms.txt, llms-full.txt, ai.txt, sitemap-ai.xml) to public/
date: 2026-04-25
status: complete
---

# Quick Task 260425-crl — Summary

## Outcome

Added four AEOrank-generated AI-discovery files to `public/`. Astro copies these verbatim to `dist/`, so they will be served at the site root after deployment:

- `https://bhagavad.net/llms.txt`
- `https://bhagavad.net/llms-full.txt`
- `https://bhagavad.net/ai.txt`
- `https://bhagavad.net/sitemap-ai.xml`

## Files added

| File | Size | Purpose |
|------|------|---------|
| `public/llms.txt` | 65,496 B | Standard llms.txt index for AI crawlers |
| `public/llms-full.txt` | 810,457 B | Full-content variant for AI crawlers |
| `public/ai.txt` | 406 B | AI training/inference licensing (CC-BY-4.0) |
| `public/sitemap-ai.xml` | 60,129 B | Supplementary sitemap with `<ai:summary>` per URL |

## Files explicitly skipped (from /tmp/aeorank/)

- `CLAUDE.md` — AEOrank's audit report (would conflict with project CLAUDE.md)
- `schema.json` — trivial Organization+WebSite JSON-LD; site already ships richer JSON-LD (commit 73cb859)
- `robots-patch.txt` — confirms existing crawlers already allowed; no-op
- `faq-blocks.html` — empty template, no FAQ content
- `citation-anchors.html` — requires manual heading-id edits across components; tracked as separate follow-up

## Verification

- `pnpm build` → 738 pages built successfully
- `dist/sitemap-index.xml` regenerated by `@astrojs/sitemap` (integration intact)
- All 4 AEO files present in `dist/` after build
- `public/robots.txt` byte-identical to before (untouched)
- `astro.config.*` not modified

## Commits

- `9408f3e` — feat(quick-260425-crl-01): add AEO discovery files (llms.txt, ai.txt, sitemap-ai)

## Deviations

One auto-fixed deviation during execution: worktree had no `node_modules`; resolved via symlink to host's `node_modules` and revert of incidental `pnpm-lock.yaml` churn. No source files affected.

## Follow-ups

- `citation-anchors.html` — add `id=` attributes to existing `<h2>`/`<h3>` headings across components for deep-link citation by AI crawlers. Touches multiple `.astro` files; warrants its own phase.
- Consider regenerating `sitemap-ai.xml` periodically (it is a frozen snapshot — content drift will accumulate).
