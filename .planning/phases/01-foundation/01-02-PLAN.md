---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/content.config.ts
  - src/data/chapters/chapter-01.json
autonomous: true
requirements:
  - CONT-08

must_haves:
  truths:
    - "npm run build validates chapter-01.json against the Zod schema with zero errors"
    - "A JSON file with Harvard-Kyoto transliteration (no IAST diacritics) fails the build"
    - "A JSON file missing required fields (sanskrit, iast, commentaries, applications, themes) fails the build"
    - "The test shloka BG 1.1 uses Gita Press verse numbering (chapter 1, verse 1)"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Zod schema for shloka content collection with IAST enforcement"
      exports: ["collections"]
      contains: "defineCollection"
    - path: "src/data/chapters/chapter-01.json"
      provides: "Test shloka BG 1.1 with all required fields"
      contains: "BG-1.1"
  key_links:
    - from: "src/content.config.ts"
      to: "src/data/chapters/chapter-01.json"
      via: "file() loader path"
      pattern: "file.*chapter-01\\.json"
    - from: "src/content.config.ts"
      to: "astro/zod"
      via: "import statement"
      pattern: "from 'astro/zod'"
    - from: "src/content.config.ts"
      to: "astro:content"
      via: "import statement"
      pattern: "from 'astro:content'"
---

<objective>
Define the Zod content schema for shloka data and validate it with a test shloka (BG 1.1).

Purpose: Locks the content contract before any AI generation begins. The schema enforces IAST transliteration, Gita Press verse numbering, and all required content fields. Invalid data fails the build.
Output: A validated content collection with one test shloka proving the schema works end-to-end.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Zod content schema with IAST enforcement</name>
  <files>src/content.config.ts</files>
  <action>
    Create `src/content.config.ts` (CRITICAL: this is the Astro 5 location -- NOT `src/content/config.ts` which is the Astro 4 path and will silently fail).

    Use ONLY `import { z } from 'astro/zod'` -- do NOT import from `zod` directly.

    ```typescript
    // src/content.config.ts
    import { defineCollection } from 'astro:content';
    import { file } from 'astro/loaders';
    import { z } from 'astro/zod';

    const shlokaSchema = z.object({
      id: z.string(),                    // "BG-1.1" format — required by file() loader
      chapter: z.number().int().min(1).max(18),
      verse: z.number().int().min(1),
      sanskrit: z.string().regex(/[\u0900-\u097F]/, {
        message: 'Sanskrit field must contain Devanagari script (U+0900-U+097F)',
      }),
      iast: z.string().regex(/[āīūṛṝḷṃḥṅñṭḍṇśṣĀĪŪṚṜḶṂḤṄÑṬḌṆŚṢ]/, {
        message: 'IAST field must use IAST diacritics (ā, ī, ū, ṛ, ṃ, ḥ, etc.) — Harvard-Kyoto not accepted',
      }),
      explanation: z.string().min(50),
      synthesis: z.string().min(100),
      commentaries: z.array(
        z.object({
          tradition: z.enum(['advaita', 'vishishtadvaita', 'bhakti']),
          teacher: z.string(),
          text: z.string().min(50),
        })
      ).min(2).max(3),
      applications: z.object({
        personal_growth: z.string().min(30),
        career_business: z.string().min(30),
        health: z.string().min(30),
        relationships: z.string().min(30),
      }),
      themes: z.array(z.string()).min(1),
    });

    export type Shloka = z.infer<typeof shlokaSchema>;

    const chapters = defineCollection({
      loader: file('src/data/chapters/chapter-01.json'),
      schema: shlokaSchema,
    });

    export const collections = { chapters };
    ```

    Key validation rules:
    - `sanskrit` field: Must contain at least one Devanagari Unicode character (U+0900-U+097F). Plain ASCII text fails.
    - `iast` field: Must contain at least one IAST diacritic character. Harvard-Kyoto (ASCII-only uppercase) fails because it has no diacritics.
    - `commentaries`: Array of 2-3 tradition-specific entries with enum-restricted tradition values.
    - `applications`: Object with all 4 domain keys required.
    - `themes`: At least 1 life-theme tag.
    - `id` field: Required by the `file()` loader — each entry needs a unique `id`.

    The `file()` loader reads a JSON array from the specified path. Each object in the array must have an `id` field.
  </action>
  <verify>
    <automated>cd /Users/vin/Development/gita-app && test -f src/content.config.ts && grep -q "defineCollection" src/content.config.ts && grep -q "astro/zod" src/content.config.ts && grep -q "file(" src/content.config.ts && echo "SCHEMA OK" || echo "SCHEMA FAILED"</automated>
  </verify>
  <done>src/content.config.ts exists at the correct Astro 5 location, imports from astro/zod (not zod directly), uses file() loader pointing to chapter-01.json, defines shlokaSchema with all required fields including IAST diacritic regex and Devanagari regex, and exports collections.</done>
</task>

<task type="auto">
  <name>Task 2: Create test shloka JSON (BG 1.1) and validate build</name>
  <files>src/data/chapters/chapter-01.json</files>
  <action>
    Create `src/data/chapters/chapter-01.json` with one test shloka (BG 1.1) using Gita Press verse numbering:

    ```json
    [
      {
        "id": "BG-1.1",
        "chapter": 1,
        "verse": 1,
        "sanskrit": "धृतराष्ट्र उवाच |\nधर्मक्षेत्रे कुरुक्षेत्रे समवेता युयुत्सवः |\nमामकाः पाण्डवाश्चैव किमकुर्वत सञ्जय ॥१॥",
        "iast": "dhṛtarāṣṭra uvāca |\ndharmakṣetre kurukṣetre samavetā yuyutsavaḥ |\nmāmakāḥ pāṇḍavāścaiva kimakurvata sañjaya ||1||",
        "explanation": "King Dhritarashtra, blind and anxious, asks his minister Sanjaya to describe what is happening on the sacred battlefield of Kurukshetra, where his sons (the Kauravas) and the Pandavas have assembled ready to fight.",
        "synthesis": "The opening question frames the entire Gita: can dharma (righteous duty) be upheld even when it requires fighting one's own family? Dhritarashtra's choice of the word 'dharmakshetra' (field of dharma) alongside 'Kurukshetra' (field of the Kurus) reveals the dual nature of the conflict — it is simultaneously a physical battle and a spiritual inquiry into the nature of right action. The blind king's question carries a subtext of fear: he senses that dharma will not favor his sons.",
        "commentaries": [
          {
            "tradition": "advaita",
            "teacher": "Adi Shankaracharya",
            "text": "The Advaita tradition notes that the battlefield is called a field of dharma because dharma itself will be the decisive factor. The question is not merely military but metaphysical — it asks what happens when duty and attachment collide."
          },
          {
            "tradition": "vishishtadvaita",
            "teacher": "Ramanujacharya",
            "text": "The Vishishtadvaita perspective emphasizes that Kurukshetra is a place made sacred by ancient rites and divine presence. The fact that both armies gathered here suggests a divine orchestration of events toward a righteous outcome."
          },
          {
            "tradition": "bhakti",
            "teacher": "Traditional Bhakti perspective",
            "text": "From the bhakti perspective, the Lord's very presence on the battlefield transforms a war into a sacred event. The question itself sets in motion Arjuna's eventual surrender to Krishna's guidance — devotion born from crisis."
          }
        ],
        "applications": {
          "personal_growth": "Every significant life decision places us on our own 'Kurukshetra' — a field where competing values and loyalties must be reconciled. Clarity about your core dharma is the starting point for navigating any inner conflict.",
          "career_business": "Business decisions often require choosing between loyalty to individuals (team members, partners) and loyalty to principles (ethics, mission). Identifying your organization's 'dharmakshetra' — its core values — helps navigate these conflicts with integrity.",
          "health": "Physical and mental health often require facing uncomfortable truths about habits and relationships. The first step, like Dhritarashtra's question, is simply acknowledging that a conflict exists between where you are and where you need to be.",
          "relationships": "Conflict in relationships is rarely only about the surface issue. Asking the deeper question — what values are actually in tension here? — opens the path to genuine resolution rather than temporary compromise."
        },
        "themes": ["duty", "conflict", "decision-making", "family", "dharma"]
      }
    ]
    ```

    Key points:
    - `id` field is "BG-1.1" (required by file() loader).
    - `chapter: 1, verse: 1` uses Gita Press numbering (CONT-08).
    - Sanskrit text contains Devanagari characters (passes regex).
    - IAST text contains diacritics like ṛ, ā, ṣ, ṃ, ñ (passes IAST regex).
    - Commentary texts are paraphrased synthesis of philosophical traditions, NOT reproductions of copyrighted text.
    - All 4 application domains present with 30+ character strings.
    - Themes array has 5 tags.

    After creating the JSON file, run `npm run build` to verify the schema validates successfully. The build MUST complete with zero errors.

    If the build fails with schema validation errors, fix the JSON data to match the schema requirements.
  </action>
  <verify>
    <automated>cd /Users/vin/Development/gita-app && npm run build 2>&1 | tail -10 && echo "---" && node -e "const d=require('./src/data/chapters/chapter-01.json'); console.log('Entries:', d.length, 'ID:', d[0].id, 'Has IAST:', /[āīūṛṝḷṃḥ]/.test(d[0].iast))" && echo "VALIDATION OK" || echo "VALIDATION FAILED"</automated>
  </verify>
  <done>chapter-01.json contains BG 1.1 with all required fields. `npm run build` succeeds with schema validation passing. The test shloka uses Gita Press verse numbering (chapter 1, verse 1), Devanagari Sanskrit, IAST transliteration with diacritics, 3 tradition commentaries, 4 application domains, and 5 theme tags.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors (schema validation passes)
2. `src/content.config.ts` is at the Astro 5 location (not src/content/config.ts)
3. Schema imports from `astro/zod` (not standalone `zod`)
4. Schema uses `file()` loader from `astro/loaders`
5. chapter-01.json contains exactly one entry with id "BG-1.1"
6. IAST field contains IAST diacritics (not Harvard-Kyoto ASCII)
7. Sanskrit field contains Devanagari Unicode characters
8. All commentary traditions are from the allowed enum: advaita, vishishtadvaita, bhakti
</verification>

<success_criteria>
- The content schema is defined and validates at build time
- IAST enforcement rejects non-IAST transliteration
- Devanagari enforcement rejects plain ASCII in the Sanskrit field
- Gita Press verse numbering is used (BG 1.1 = chapter 1, verse 1)
- The test shloka passes all validation rules
- The build succeeds end-to-end with the schema and test data
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
