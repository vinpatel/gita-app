---
phase: 01.1-life-problem-search
plan: 02
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - src/pages/explore/index.astro
  - src/pages/index.astro
autonomous: false
requirements:
  - UX-03
  - UX-04

must_haves:
  truths:
    - "A user visiting /explore/ sees a prominent search box and can type a life problem to find relevant verses"
    - "The homepage has a search input in the hero section that links to or embeds the search experience"
    - "Search results display verse reference, explanation snippet, and theme tags with links to verse pages"
    - "The explore page loads and the search island hydrates without JavaScript errors"
  artifacts:
    - path: "src/pages/explore/index.astro"
      provides: "Dedicated search/explore page with LifeSearch island"
      contains: "LifeSearch"
    - path: "src/pages/index.astro"
      provides: "Homepage with search entry point in hero section"
      contains: "LifeSearch"
  key_links:
    - from: "src/pages/explore/index.astro"
      to: "src/components/islands/LifeSearch.tsx"
      via: "Preact island with client:load"
      pattern: "LifeSearch.*client:load"
    - from: "src/pages/index.astro"
      to: "src/components/islands/LifeSearch.tsx"
      via: "Preact island with client:idle or inline search link"
      pattern: "LifeSearch|explore"
---

<objective>
Create the `/explore/` search page and add a search entry point to the homepage, wiring both to the LifeSearch island.

Purpose: Makes the search feature discoverable and usable — the explore page is the primary search destination, the homepage provides a quick entry point.
Output: `/explore/` page with full search experience, homepage with search integration.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-life-problem-search/01.1-RESEARCH.md
@.planning/phases/01.1-life-problem-search/01.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/components/islands/LifeSearch.tsx:
```tsx
// Props: { baseUrl: string }
// Usage: <LifeSearch baseUrl={import.meta.env.BASE_URL} client:load />
export default function LifeSearch({ baseUrl }: { baseUrl: string }): JSX.Element;
```

From src/pages/index.astro (existing structure):
```astro
---
import BaseLayout from '@layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
const allShlokas = await getCollection('chapters');
// ... chapterMeta array, hero section, chapter grid
---
```

Existing layout: BaseLayout.astro wraps all pages.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /explore/ page and add search to homepage</name>
  <files>src/pages/explore/index.astro, src/pages/index.astro</files>
  <action>
1. Create `src/pages/explore/index.astro`:
   - Import `BaseLayout` from `@layouts/BaseLayout.astro`
   - Import `LifeSearch` from `@components/islands/LifeSearch.tsx`
   - Page title: "Explore the Gītā — Find Wisdom for Life's Challenges"
   - Page structure:
     - Hero section with heading: "What are you struggling with?" (h1, large, centered)
     - Subtext: "The Bhagavad Gītā has been answering life's hardest questions for 5,000 years. Type your question below." (subtle, centered)
     - `<LifeSearch baseUrl={import.meta.env.BASE_URL} client:load />` — hydrate immediately since search is the page's purpose
     - Below the search: a brief explanation section with 3-4 example queries as styled text (e.g. "Try: 'I feel lost', 'how to deal with anger', 'fear of failure', 'letting go'")
   - Wrap in BaseLayout with appropriate title and description meta
   - Styling: generous whitespace, centered content, max-w-2xl or similar, serene aesthetic matching the rest of the site

2. Modify `src/pages/index.astro`:
   - Import `LifeSearch` from `@components/islands/LifeSearch.tsx`
   - Add a search section in the hero area (between the existing hero heading and the chapter grid)
   - Include `<LifeSearch baseUrl={import.meta.env.BASE_URL} client:idle />` — use `client:idle` on homepage (not the primary purpose of the page, so defer hydration)
   - Add a small link below the search input: "Or explore all life questions →" linking to `/explore/` (with BASE_URL prefix via an `<a>` tag)
   - Keep the existing chapter grid intact — search is an addition, not a replacement
   - Preserve all existing functionality (chapter cards, metadata, etc.)

Styling notes:
- The explore page should feel calm and inviting — this is where someone comes when they're struggling
- Use Tailwind prose-like spacing: py-12 or py-16 for sections
- Search input on explore page should be larger/more prominent than on homepage
- Homepage search can be more compact to fit within the hero without overwhelming the chapter grid
  </action>
  <verify>
    <automated>cd /Users/vin/Development/gita-app && npm run build 2>&1 | tail -10 && test -d dist/explore && grep -l "LifeSearch" src/pages/explore/index.astro src/pages/index.astro</automated>
  </verify>
  <done>The /explore/ page exists with LifeSearch island (client:load), the homepage includes LifeSearch (client:idle) with a link to /explore/, and both build without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify search experience end-to-end</name>
  <files>n/a</files>
  <action>
Human verification of the complete search feature. What was built:
- Build-time search index endpoint (search-index.json) with all 701 verses
- LifeSearch Preact island with Fuse.js fuzzy search, ARIA combobox, keyboard navigation
- /explore/ dedicated search page with prominent search experience
- Homepage integration with search input in hero section

How to verify:
1. Run `npm run dev` and open http://localhost:4321/
2. On the homepage, verify a search input is visible in the hero section
3. Click the search input — theme suggestion pills should appear (grief, anger, duty, etc.)
4. Click a theme pill (e.g. "grief") — verse results should appear
5. Clear the input and type "I feel overwhelmed" — relevant verses should appear
6. Click a result — it should navigate to the correct verse page (e.g. /verse/2/1/)
7. Navigate to http://localhost:4321/explore/
8. Verify the dedicated search page has a large, prominent search box
9. Type "fear of failure" — results should appear with relevant verses
10. Use keyboard: ArrowDown to navigate results, Enter to select, Escape to close
11. Verify the search works with typos (e.g. "greif" should still find grief-related verses)
  </action>
  <verify>
    <automated>cd /Users/vin/Development/gita-app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Human approves the search experience — search returns relevant results, navigation works, and the UX feels right</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `dist/explore/index.html` exists
3. Homepage HTML includes LifeSearch island reference
4. Human verifies interactive search works end-to-end in dev mode
</verification>

<success_criteria>
- /explore/ page renders with prominent search experience
- Homepage includes search entry point without breaking existing chapter grid
- Search returns relevant results for life problem queries
- Keyboard navigation and ARIA attributes function correctly
- Human approves the search experience
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-life-problem-search/01.1-02-SUMMARY.md`
</output>
